export const showLoginSpinner = (bool) => ({
    type: 'SHOW_LOGIN_SPINNER',
    bool
});

export const showAddInstumSpinner = (bool) => ({
    type: 'SHOW_INSTRUMENT_SPINNER',
    bool
});

export const showSaveSpinner = (bool) => ({
    type: 'SHOW_SAVE_SPINNER',
    bool
});

export const showSubmitSpinner = (bool) => ({
    type: 'SHOW_SUBMIT_SPINNER',
    bool
});

export const showCancelSpinner = (bool) => ({
    type: 'SHOW_CANCEL_SPINNER',
    bool
});

export const showDeleteSpinner = (bool) => ({
    type: 'SHOW_DELETE_SPINNER',
    bool
});